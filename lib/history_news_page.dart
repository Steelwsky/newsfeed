import 'package:flutter/material.dart';

class HistoryNewsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      key: ValueKey('historyNewsPage'),
      child: Center(
        child: Text('History page here'),
      ),
    );
  }
}